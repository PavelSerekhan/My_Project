package Senla.adminHotel.api.dao;

import Senla.adminHotel.model.Guest;
import java.util.List;

public interface IGuestDao extends GenericDao<Guest> {

    // методы были тут до extends GenericDao<Guest>
//    void save(Guest entity);
//    Guest getById(long id);
//    List<Guest> getAll();
//    void delete(Guest entity);
//    Guest update(Guest entity);
}
